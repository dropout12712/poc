{
  "name": "secure-storage-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for secure encrypted storage with zero server-side plaintext visibility",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "jest"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}